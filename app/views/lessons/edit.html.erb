<div class="card">
  <div class="card-header text-center ">
    <h1>Editing lesson</h1>
  </div>

  <div class="card-body">
    <%= render "form", lesson: @lesson %>
  </div>

  <div class="card-footer">
    <%= link_to "Show this lesson", course_lesson_path(@course, @lesson), class: "btn btn-outline-primary btn-sm" %> 
    <%= link_to "Back to lessons", course_lessons_path(@course), class: "btn btn-info btn-sm" %>
  </div>
</div>