<div class="card">
  <div class="card-header"><h3><i class="fa-solid fa-lock-open"></i> Buy Course</h3></div>
  <div class="card-body">
    <%= form_tag course_enrollments_path(@course) do %>
      <%= submit_tag "Pay #{@course.price}", class: "btn btn-success btn-sm" %>
    <% end %>
  </div>
  <div class="card-footer">
    
  </div>
</div>
