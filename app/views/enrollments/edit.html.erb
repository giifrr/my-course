<%= link_to "Enrollments", enrollments_path %> > 
<%= "Edit Enrollment" %> 


<div class="card">
  <div class="card-header"><h3><i class="fa-solid fa-lock-open"></i> Editing enrollment</h3></div>
  <div class="card-body"><%= render "form", enrollment: @enrollment %></div>
  <div class="card-footer">
    <%= link_to "Show this enrollment", @enrollment, class: "btn btn-primary btn-sm" %> 
    <%= link_to "Back to enrollments", enrollments_path, class: "btn btn-info btn-sm" %>
  </div>
</div>
